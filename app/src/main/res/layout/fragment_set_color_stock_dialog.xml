<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="grocerylist.india.com.materialdesign.dialog.SetColorStockDialog">

    <TextView
        android:id="@+id/select_stock_title"
        android:layout_width="match_parent"
        android:layout_height="30dp"
        android:layout_alignParentTop="true"
        android:layout_marginLeft="12dp"
        android:text="Update Stock"
        android:textColor="@color/bright_foreground_material_light"
        android:textSize="24sp" />

    <TextView
        android:id="@+id/select_color_stock_ok"
        android:layout_width="wrap_content"
        android:layout_height="30dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_marginRight="10dp"
        android:gravity="center"
        android:paddingLeft="15dp"
        android:paddingRight="15dp"
        android:text="OK"
        android:textColor="@color/colorPrimary" />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/color_stock_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/select_color_stock_ok"
        android:layout_below="@id/select_stock_title"
        android:visibility="visible" />

</RelativeLayout>
